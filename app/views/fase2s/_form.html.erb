<%= form_with(model: fase2, local: true) do |form| %>
  <% if fase2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fase2.errors.count, "error") %> prohibited this fase2 from being saved:</h2>

      <ul>
      <% fase2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="card">
  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Criar nova", class: 'btn' %>

</div>
<% end %>
